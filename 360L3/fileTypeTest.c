#include <stdio.h>
#include <string.h>

int main(int argc, char** argv){
	// I will make the assumption that the file being examined is always in the current directory
	char file[50] = "./";
	strcat(file, argv[1]); // ./(FILE)
	
	
	
		
	return 0;
}
